@{
    ViewBag.Title = "Dashboard";
}


<script src="~/Scripts/jquery-1.10.2.js"></script>
<script src="~/Scripts/bootstrap.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<div class="page-content">
    <div class="row">
        <div class="col-md-12">


            @using (Ajax.BeginForm("SearchRecipes", new AjaxOptions()
                {
                    UpdateTargetId = "searchResult",
                    HttpMethod = "Get",
                    InsertionMode = InsertionMode.Replace,

                }))
            {

                <div class="form-group">
                    @Html.TextBox("keyword", null, new { @class = "col-sm-2 ", placeholder = "Enter Recipe Title" })
                    <button type="submit" class="col-sm-1 btn-success">Search</button>
                </div>

                <br><br>


                <div class="content-box-large box-with-header">
                    <div class="content-box-header">
                        <center><h2>Recipe Table</h2></center>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Title</th>
                                    <th>Image</th>
                                    <th>Prepration Time</th>
                                    <th>Cooking Time</th>
                                    <th>Action</th>

                                </tr>
                            </thead>
                            <tbody id="searchResult">
                                @Html.Action("SearchRecipes", new { keyword = "" })
                            </tbody>
                        </table>
                    </div>
                    <br /><br />
                </div>
            }
        </div>
    </div>
</div>


